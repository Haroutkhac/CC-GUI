# Bug Hunt Audit Report - 2026-03-16

## Summary
Full audit of CC-GUI server and frontend code. 15 bugs fixed, 0 hypothesis failures.

## Bugs Found & Fixed

### HIGH Severity
| # | Bug | File | Fix |
|---|-----|------|-----|
| 1 | No graceful shutdown - orphaned pty processes | server/index.js | Added SIGTERM/SIGINT handlers calling killAll() |
| 2 | Canvas click/touch listeners leak on unmount | engine.js | Stored refs, added to _cleanup function |
| 3 | ESC key doesn't close terminal (xterm captures it) | App.jsx | Capture phase event listener |
| 4 | WASD keys captured when typing in form inputs | engine.js | Guard against focused input/textarea elements |
| 5 | Store save race condition | store.js | Debounced writes (100ms) |

### MEDIUM Severity
| # | Bug | File | Fix |
|---|-----|------|-----|
| 6 | Dead code: broken ternary lerp overwritten | engine.js:291 | Removed dead code |
| 7 | Socket event listeners not removed on cleanup | useSocket.js | Added socket.off() for all events |
| 8 | Sprite caches grow unbounded (memory leak) | sprites.js | LRU eviction at 100 entries (both caches) |
| 9 | statusBuffers leak for deleted sessions | server/index.js | Cleanup on session delete + orphan detection |
| 10 | Status detection false positives (❯ anywhere) | server/index.js | Match only at end of buffer |
| 11 | Missing input validation on terminal:input | server/index.js | Type check for string |
| 12 | Missing validation on terminal:resize | server/index.js | Integer + positive checks |
| 13 | Pokemon sprites blocking project labels | engine.js | Labels drawn last, NPCs repositioned below/beside |

### LOW Severity
| # | Bug | File | Fix |
|---|-----|------|-----|
| 14 | _clickTarget not cleared on destroy | engine.js | Set to null + clear keys |
| 15 | Empty command crashes terminal spawn | server/index.js | Trim + guard |

## Not Fixed (Accepted Risk)
- CORS wildcard (`origin: '*'`) - acceptable for local/Tailscale use
- No project count limits - acceptable for personal tool
- Scrollback memory (100KB cap already in place)

## Failure Count: 0
All fixes confirmed on first attempt.
